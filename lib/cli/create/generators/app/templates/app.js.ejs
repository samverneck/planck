import * as Planck from '@node/planck';
import AppRouterHTTP from './routers/app-router-http';

async function main(){
	const app = await new Planck.App();
	app.use(AppRouterHTTP);
}

main().catch(e => {console.log (e)});
