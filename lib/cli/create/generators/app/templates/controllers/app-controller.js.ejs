import * as Planck from '@node/planck';

@Controller.useViews('json-wrap-view')
class AppController extends Planck.Controller.Base{
	constructor(){
		super();
	}

	validate(params){
        //	if (not valid){
        //		throw new Error('params not valid');
        //	}
		return true;
	}
}

export default AppController;
