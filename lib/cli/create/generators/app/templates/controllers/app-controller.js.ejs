import * as Planck from 'planck';

@Planck.Controller.useViews('json')
class AppController extends Planck.Controller.Base{
	constructor(){
		super();
	}

	validate(params){
        //	if (not valid){
        //		throw new Error('params not valid');
        //	}
		return true;
	}
}

export default AppController;
