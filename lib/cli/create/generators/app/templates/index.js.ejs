require('systemjs');

System.config({
    defaultJSExtensions: true,
	transpiler: 'babel',
	babelOptions: {
		'stage': 0,
		'blacklist': [
			'es3.memberExpressionLiterals',
			'es3.propertyLiterals',
			'es5.properties.mutators',
			'es6.templateLiterals'
		]
	}
});

System.import('./app').catch(e => {
    console.log(e)
});
