<%%
let ModelName = name.charAt(0).toUpperCase() + name.slice(1);
-%%>
import {ActiveRecord} from 'planck';
//TODO: doc url
//auto-generated model for <%%=name%%>
class <%%=ModelName%%> extends ActiveRecord.Base{
	id = 0;
	constructor(params){
		super(params);
	}
}

<%%=ModelName%%>.init();

export default <%%=ModelName%%>;
